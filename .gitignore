# ── Environment files ────────────────────────────────────────────
.env
.env.prod
.env.local
services/auth/.env
services/auth/.env.prod
services/gateway/.env
services/gateway/.env.prod

# ── Go ───────────────────────────────────────────────────────────
# binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/

# test output
*.test
*.out
coverage.html
coverage.out

# go workspace sum (some teams commit this, some don't)
# we commit it — it ensures reproducible builds
# go.work.sum  ← do NOT ignore this

# ── Air hot reload ───────────────────────────────────────────────
services/auth/tmp/
services/gateway/tmp/
tmp/

# ── SQLC generated (optional — some teams commit generated code)
# services/auth/internal/adapter/postgres/queries/
# we commit generated code so teammates don't need sqlc installed

# ── Proto generated (same — we commit it)
# shared/proto/gen/

# ── Docker ───────────────────────────────────────────────────────
# nothing to ignore here, Dockerfiles are committed

# ── IDE ──────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
.DS_Store
#air tmep
*/tmp/
build-errors.log

# ── OS ───────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Logs ─────────────────────────────────────────────────────────
*.log
logs/

# ── Test / coverage ──────────────────────────────────────────────
coverage/

# ── Misc ──────────────────────────────────────────────
MoneyMate_Technical_Architecture_Documentation.pdf
doc.md
STEP2_GATEWAY_PLAN.md
STEP2_TOPICS_TO_LEARN.md
STEP3_GRPC_TO_REST_PLAN.md
IMPLEMENTATION_PLAN.md
